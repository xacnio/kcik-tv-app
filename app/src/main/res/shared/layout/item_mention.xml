<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_bottom_sheet_item"
    android:padding="12dp"
    android:layout_marginBottom="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/mentionUsername"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            tools:text="mentionUsernameUsername"
            android:textColor="@color/brand_green"
            android:textStyle="bold"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/mentionTimestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="00:00"
            android:textColor="#888888"
            android:textSize="12sp"
            android:layout_marginEnd="12dp" />

        <ImageView
            android:id="@+id/btnDismissMention"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_close"
            android:padding="2dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/celebration_action_dismiss"
            android:tint="#888888" />
    </LinearLayout>

    <TextView
        android:id="@+id/mentionContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        tools:text="mentionContent"
        android:textColor="#FFFFFF"
        android:textSize="14sp" />

</LinearLayout>
